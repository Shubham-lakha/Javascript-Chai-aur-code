"use strict" //treat all code as newer version of js
// alert(2+2) //we are using node js , not browser

console.log(3
    + //It is not a good practice, code readability shuould be high
    3)

    console.log("Lakha");

    let age = 22
    let isloggedIn = false
    let state; //now it is undefined 
    state =null // now it doesn't have any value

    // number --> 2 to power 53 (range)
    //bigInt --> Only use in very large number (stock market, facebook ,etc)
    // string --> ""
    // boolean --> true/false
    //null --> (typeof --> object)
    //undefined

    console.log(typeof age);
